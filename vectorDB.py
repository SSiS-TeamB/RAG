# chroma_client = ChromaClient()

# Settings for semantic_search using "chromadb" module
# emb_func = STEF(model_name=base_model, normalize_embeddings=True)
{'collection_name': 'wf_schema', 'persist_directory': './chroma_storage'}

# client


